
╔══════════════════════════════════════════════════════════════════╗
║                                                                  ║
║              🚀 BACKEND SETUP - FOLLOW THESE STEPS               ║
║                                                                  ║
╚══════════════════════════════════════════════════════════════════╝

⚠️  BEFORE YOU START THE BACKEND, YOU NEED 3 API KEYS!
════════════════════════════════════════════════════════

The backend CANNOT run without these keys. Get them now (5 minutes):

1. 🔑 OpenAI API Key
   → Go to: https://platform.openai.com/api-keys
   → Create account → Click "Create new secret key"
   → Copy the key (starts with sk-)
   → Cost: ~$5-10/month

2. 🔑 Qdrant Vector Database
   → Go to: https://cloud.qdrant.io/
   → Create free account → Create a cluster
   → Copy Cluster URL and API Key
   → Cost: FREE (1GB included)

3. 🔑 Cloudinary File Storage
   → Go to: https://cloudinary.com/
   → Create free account → Go to Dashboard
   → Copy: Cloud Name, API Key, API Secret
   → Cost: FREE (25GB included)


📋 STEP-BY-STEP SETUP
═════════════════════

Step 1: Create Virtual Environment
───────────────────────────────────
cd backend
python -m venv venv

# Activate it:
# Mac/Linux:
source venv/bin/activate

# Windows:
venv\Scripts\activate

You should see (venv) in your prompt.


Step 2: Install Dependencies (2-3 minutes)
──────────────────────────────────────────
pip install -r requirements.txt

Wait for it to finish installing all packages.


Step 3: Create .env File with YOUR API Keys
───────────────────────────────────────────
cp .env.example .env

# Edit the file (choose one):
nano .env
# or
code .env
# or
vim .env

ADD YOUR REAL API KEYS (remove "your_" placeholders):
─────────────────────────────────────────────────────

OPENAI_API_KEY=sk-your-actual-openai-key-here
QDRANT_URL=https://your-cluster.qdrant.io
QDRANT_API_KEY=your-actual-qdrant-key
CLOUDINARY_CLOUD_NAME=your-actual-cloud-name
CLOUDINARY_API_KEY=your-actual-cloudinary-key
CLOUDINARY_API_SECRET=your-actual-cloudinary-secret

⚠️  WITHOUT THESE REAL KEYS, THE BACKEND WON'T START!


Step 4: Initialize Qdrant Database
──────────────────────────────────
python init_qdrant.py

Should see: ✅ Qdrant collection initialized successfully!


Step 5: START THE BACKEND!
──────────────────────────
uvicorn app.main:app --reload

Success looks like:
✅ INFO:     Uvicorn running on http://127.0.0.1:8000
✅ INFO:     Application startup complete.

BACKEND IS NOW RUNNING! ✅


🧪 TEST IT WORKS
═══════════════

Open in browser: http://localhost:8000

Should see:
{"message": "AI Document Chatbot API", "status": "running"}

Or visit API docs: http://localhost:8000/docs


❌ IF YOU GET ERRORS
════════════════════

"ValidationError" or "OPENAI_API_KEY not found"
→ Your .env file is missing or keys are wrong
→ Check: ls -la backend/.env
→ Make sure you replaced ALL "your_" placeholders

"Connection to Qdrant failed"
→ Qdrant URL or API key is wrong
→ Check your Qdrant cluster is active
→ Run: python init_qdrant.py again

"No module named 'app'"
→ You're not in the backend directory
→ Or venv is not activated
→ Check prompt shows (venv)


🎯 QUICK COMMANDS
═════════════════

FIRST TIME:
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# Edit .env with real API keys!
python init_qdrant.py
uvicorn app.main:app --reload

EVERY TIME AFTER:
cd backend
source venv/bin/activate
uvicorn app.main:app --reload


✅ CHECKLIST
════════════

Before starting backend:
□ Have OpenAI API key
□ Have Qdrant URL + API key  
□ Have Cloudinary credentials
□ Created venv
□ Installed dependencies
□ Created .env with REAL keys
□ Initialized Qdrant

Backend is ready when:
□ No errors on startup
□ Can access http://localhost:8000
□ API docs work at /docs


🚀 NEXT: START FRONTEND
════════════════════════

Keep backend running, open NEW terminal:

bun run dev

Then open: http://localhost:3000


═══════════════════════════════════════════════════════════════

📖 Full Guide: Read BACKEND_SETUP_GUIDE.md
🆘 Problems?: Check SETUP_CHECKLIST.md

═══════════════════════════════════════════════════════════════
